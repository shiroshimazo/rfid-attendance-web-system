# Reports service

Role-scoped report queries and complete PDF export of the documented attendance,
RFID, and authorized SMS content. Existing browser printing and print CSS can be
retained when the exported document covers the intended report scope.

Server-side generation and a new PDF library are not requirements. Preserve
teacher assignment boundaries and existing guardian-message permissions; required
report content does not automatically authorize broader teacher SMS access.
